[package]
name = "rust-nms"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_nms"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
numpy = "0.20"
ndarray = "0.15"

[profile.release]
lto = true
codegen-units = 1
