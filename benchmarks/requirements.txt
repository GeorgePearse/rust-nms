# Benchmarking dependencies
numpy>=1.20.0
pandas>=1.3.0
plotly>=5.0.0
kaleido>=0.2.1  # Required for static image export

# Note: rust_nms must be built separately with:
# pip install maturin
# maturin develop --release
